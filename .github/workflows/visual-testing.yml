name: Visual Regression Testing

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  chromatic-visual-tests:
    name: Chromatic Visual Regression
    runs-on: ubuntu-latest
    if: false  # TEMPORARILY DISABLED - Requires CHROMATIC_PROJECT_TOKEN secret

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 10



      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Storybook
        run: npm run build-storybook

      - name: Run Chromatic Visual Tests
        uses: chromaui/action@latest
        with:
          projectToken: ${{ secrets.CHROMATIC_PROJECT_TOKEN }}
          buildScriptName: 'build-storybook'
          onlyChanged: true
          exitZeroOnChanges: true
          storybookBuildDir: 'storybook-static'
          autoAcceptChanges: ${{ github.ref == 'refs/heads/main' }}
          diagnostics: true
          junitReport: true
          skip: 'dependabot/**'

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: chromatic-test-results
          path: test-results/
          retention-days: 30

  cross-browser-tests:
    name: Cross-Browser Testing
    runs-on: ubuntu-latest
    if: false  # TEMPORARILY DISABLED - Re-enable after fixing worker configuration

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Build Storybook
        run: npm run build-storybook

      - name: Run Playwright cross-browser tests
        run: npm run test:cross-browser
        env:
          CI: true

      - name: Run Playwright visual tests (backup)
        run: npm run visual:local
        env:
          CI: true
        continue-on-error: true

      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30

      - name: Upload visual test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: visual-test-results
          path: visual-test-results/
          retention-days: 30

  performance-tests:
    name: Performance Testing
    runs-on: ubuntu-latest
    if: false  # TEMPORARILY DISABLED - Focus on unit tests first

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run bundle analysis
        run: npm run test:bundle-size

      - name: Build for performance testing
        run: npm run build

      - name: Run performance tests
        run: npm run test:performance

      - name: Upload performance results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: performance-results
          path: performance-results/
          retention-days: 30
