#!/usr/bin/env bash

# Commitlint validation - ensures conventional commit format
# DISABLED: commitlint has ES module loading issues with current Node.js version
# Can be re-enabled once commitlint is properly configured
# npx --no -- commitlint --edit $1
